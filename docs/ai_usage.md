# AI usage
Mostly I tried not to use AI tools, in the backend no AI was used.

## NewBookingView
The layout was created by an AI, but only the layout, everything else is my creation. 

## db.changelog-sample-data.yml
Didn't have time to create my own and as it is just for testing the features, it shouldn't really impact the project that much.  

## Code reviews
For the NewBookingView, AI was used to catch mistakes in the layout and code. It wasn't used extensively, but I ran the code through it once and selected what things to add and waht not to add:
1. It caught that there were no try-catch statements for await functions
2. It gave arecommendation to create a custom hook for state handling
3. Redundant useState usage for "ready to submit" validation

## Layout and html
As I am not that great with html nor CSS, a lot of questions were asked from AI about these things.
